#include<bits/stdc++.h>
#define int long long
using namespace std;
const int N = 30;
set<int> s;
int path[N];
int ans = 0;


signed main(){
    int t;
    cin >> t;
    while(t--){
        string x; cin >> x;
        int y = pow(10, x.length()) + 1;

        cout << y << endl;
    
    }

    return 0;
}